spring.application.name=Invoice-API

spring.datasource.url=jdbc:postgresql://localhost:5432/Invoice-billing
spring.datasource.username=postgres
spring.datasource.password=himath

# JPA (Hibernate) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.sql.init.mode=always

# Logging SQL Queries (optional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
#app.expense.csv.storage.path=expense-csv